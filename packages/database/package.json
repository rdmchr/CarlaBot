{
  "name": "@carla/database",
  "version": "0.0.0",
  "main": "index.js",
  "type": "module",
  "devDependencies": {
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "prisma": "^4.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prisma:migrate": "npx prisma migrate dev"
  }
}
